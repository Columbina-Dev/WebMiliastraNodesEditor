import type { NodeDefinition } from '../types/node';

/**
 * TODO 添加服务器节点图可用的节点定义。
 * 服务器节点定义将在未来版本补充。
 */
export const serverNodeDefinitions: NodeDefinition[] = [
  // TODO: 在此处添加服务器节点定义
];

export default serverNodeDefinitions;

